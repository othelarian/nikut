use winger{WindowDim, WindowOpt, WinSurface};

fn main() {
    //
    let win = WinSurface::new{
        WindowDim::windowed(800, 400),
        "Luminance snd try",
        WindowOpt::default()
    };
    //
    //
}
